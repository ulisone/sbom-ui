<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= report.title %></title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif; line-height: 1.6; color: #1a1a2e; background: #fff; padding: 40px; }
    .container { max-width: 900px; margin: 0 auto; }
    h1 { font-size: 28px; margin-bottom: 8px; }
    h2 { font-size: 20px; margin: 32px 0 16px; border-bottom: 2px solid #e0e0e0; padding-bottom: 8px; }
    h3 { font-size: 16px; margin: 24px 0 12px; }
    .meta { color: #666; font-size: 14px; margin-bottom: 32px; }
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; margin: 16px 0; }
    .card { background: #f8f9fa; border-radius: 8px; padding: 20px; }
    .card-title { font-size: 12px; color: #666; text-transform: uppercase; margin-bottom: 4px; }
    .card-value { font-size: 32px; font-weight: 700; }
    .card-value.critical { color: #dc2626; }
    .card-value.high { color: #ea580c; }
    .card-value.medium { color: #ca8a04; }
    .card-value.low { color: #16a34a; }
    table { width: 100%; border-collapse: collapse; margin: 16px 0; }
    th, td { padding: 12px; text-align: left; border-bottom: 1px solid #e0e0e0; }
    th { background: #f8f9fa; font-weight: 600; }
    .badge { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 12px; font-weight: 600; }
    .badge.critical { background: #fee2e2; color: #dc2626; }
    .badge.high { background: #ffedd5; color: #ea580c; }
    .badge.medium { background: #fef3c7; color: #ca8a04; }
    .badge.low { background: #dcfce7; color: #16a34a; }
    .recommendation { padding: 16px; background: #f8f9fa; border-radius: 8px; margin: 12px 0; }
    .recommendation-title { font-weight: 600; margin-bottom: 4px; }
    .recommendation-desc { color: #666; font-size: 14px; }
    .footer { margin-top: 48px; padding-top: 24px; border-top: 1px solid #e0e0e0; color: #666; font-size: 12px; text-align: center; }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= report.title %></h1>
    <div class="meta">
      <p>Project: <%= report.project.name %></p>
      <p>Generated: <%= report.generated_at&.strftime("%Y-%m-%d %H:%M:%S") %></p>
      <p>Report Type: <%= report.report_type.titleize %></p>
    </div>

    <% content = report.content.with_indifferent_access %>
    <% vuln_summary = content[:vulnerability_summary] || {} %>

    <h2>Overview</h2>
    <div class="grid">
      <div class="card">
        <div class="card-title">Risk Score</div>
        <div class="card-value"><%= content[:risk_score] || 0 %></div>
      </div>
      <div class="card">
        <div class="card-title">Critical</div>
        <div class="card-value critical"><%= vuln_summary[:critical] || 0 %></div>
      </div>
      <div class="card">
        <div class="card-title">High</div>
        <div class="card-value high"><%= vuln_summary[:high] || 0 %></div>
      </div>
      <div class="card">
        <div class="card-title">Medium</div>
        <div class="card-value medium"><%= vuln_summary[:medium] || 0 %></div>
      </div>
      <div class="card">
        <div class="card-title">Low</div>
        <div class="card-value low"><%= vuln_summary[:low] || 0 %></div>
      </div>
    </div>

    <% if content[:top_vulnerabilities].present? || content[:all_vulnerabilities].present? %>
      <h2>Vulnerabilities</h2>
      <table>
        <thead>
          <tr>
            <th>CVE ID</th>
            <th>Severity</th>
            <th>Package</th>
            <th>Title</th>
            <th>Fixed Version</th>
          </tr>
        </thead>
        <tbody>
          <% (content[:all_vulnerabilities] || content[:top_vulnerabilities] || []).first(50).each do |vuln| %>
            <tr>
              <td><%= vuln[:cve_id] %></td>
              <td><span class="badge <%= vuln[:severity].to_s.downcase %>"><%= vuln[:severity] %></span></td>
              <td><%= vuln[:package] || vuln[:package_name] %></td>
              <td><%= vuln[:title] %></td>
              <td><%= vuln[:fixed_version] || '-' %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>

    <% if content[:recommendations].present? %>
      <h2>Recommendations</h2>
      <% content[:recommendations].each_with_index do |rec, index| %>
        <div class="recommendation">
          <div class="recommendation-title"><%= index + 1 %>. <%= rec[:title] %></div>
          <div class="recommendation-desc"><%= rec[:description] %></div>
        </div>
      <% end %>
    <% end %>

    <div class="footer">
      <p>Generated by SBOM Dashboard</p>
      <p><%= Time.current.strftime("%Y-%m-%d %H:%M:%S %Z") %></p>
    </div>
  </div>
</body>
</html>
